import Root, { type ButtonVariant, type ButtonSize } from './button.svelte';

export {
	Root,
	type ButtonVariant,
	type ButtonSize,
	Root as Button,
	/** @deprecated Use `Button` instead. */
	Root as button
};
