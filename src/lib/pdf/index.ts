/**
 * Barrel export for the client-side PDF module.
 *
 * Import `renderInvoicePdf` from `$lib/pdf` in any client component and pass an
 * `InvoiceData` snapshot to obtain downloadable PDF bytes.
 */
export { renderInvoicePdf } from './renderer';
